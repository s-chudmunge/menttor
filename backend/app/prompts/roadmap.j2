You are an expert curriculum designer. Generate a comprehensive, structured learning roadmap in JSON format based on the following user request.

**User Request:**
- **Subject:** {{ subject }}
- **Goal:** {{ goal }}
- **Time to Learn:** {{ time_value }} {{ time_unit }}

**Instructions:**
1.  Create a JSON object that strictly follows the schema described below.
2.  The `title` should be a concise and engaging title for the roadmap.
3.  The `description` should be a brief, motivating overview of what the user will learn.
4.  The `modules` array should break down the learning path into logical sections.
5.  Each `module` should have a `title`, a suggested `timeline`, and an array of `topics`.
6.  Each `topic` should have a `title` and an array of `subtopics`.
7.  Each `subtopic` must have a `title`.
8.  Based on the subtopic's nature, set the boolean flags `has_learn`, `has_quiz`, and `has_code_challenge`.
    - `has_learn` should almost always be `true`.
    - `has_quiz` should be `true` for conceptual knowledge that can be tested.
    - `has_code_challenge` should only be `true` if the subtopic involves writing or applying code.

**JSON Schema:**
```json
{
  "title": "string",
  "description": "string",
  "modules": [
    {
      "title": "string",
      "timeline": "string",
      "topics": [
        {
          "title": "string",
          "subtopics": [
            {
              "title": "string",
              "has_learn": "boolean",
              "has_quiz": "boolean",
              "has_code_challenge": "boolean"
            }
          ]
        }
      ]
    }
  ]
}
```

**Example:**
```json
{
  "title": "Mastering Python for Web Development",
  "description": "An 8-week journey to build powerful web applications with Python, Flask, and Django.",
  "modules": [
    {
      "title": "Module 1: Python Fundamentals",
      "timeline": "Weeks 1-2",
      "topics": [
        {
          "title": "Topic 1: Basic Syntax and Data Types",
          "subtopics": [
            {
              "title": "Variables, Expressions, and Statements",
              "has_learn": true,
              "has_quiz": true,
              "has_code_challenge": true
            },
            {
              "title": "Introduction to Data Structures: Lists, Tuples, Dictionaries",
              "has_learn": true,
              "has_quiz": true,
              "has_code_challenge": true
            }
          ]
        }
      ]
    }
  ]
}
```

Begin JSON response:
